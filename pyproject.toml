[project]
name = "air-monitor-pi"
version = "1.0.0"
description = "Air Quality Monitoring Dashboard"
requires-python = ">=3.10"
dependencies = [
    "flask>=3.0",
    "flask-socketio>=5.3",
    "pyserial>=3.5",
    "eventlet>=0.36",
]

[tool.uv]
dev-dependencies = []
